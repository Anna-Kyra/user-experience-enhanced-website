<%- include('partials/head', {current: 'vragenlijst'}); %>
<%- include('partials/vragenlijst_header', {current: 'scores'}); %>
<main>
    <form method="POST" action="/klant-toevoegen-scores/<%= companies.id %>">
       
        <fieldset>
            <legend>Scores toevoegen</legend>
            <i>Van de gekozen sdgs score van -5 tot 5</i>
            <div class="sdgs selected">
                <%- include('partials/selected_sdgs'); %>
            </div>
        </fieldset>

        <footer>
            <!-- action="/klant-toevoegen-scores/<%= companies.id %>" -->
            <a href="/klant-toevoegen-sdgs/<%= companies.id %>"> < Terug</a>
            <div>
                <a href="/dashboard/<%= companies.id %>" class="skip">Overslaan</a>
                <button type="submit" class="sdgs-button">Afronden > </button>
            </div>
        </footer>
    </form>
</main>
<footer>

</footer>
<%- include('partials/foot') %>