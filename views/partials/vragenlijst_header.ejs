<header>
    <section>
        <ul>
            <li><a href="/dashboard/<%= companies.id %>">Dashboard</a></li>
            <li>Klant toevoegen</li>
        </ul>

        <a href="/dashboard/<%= companies.id %>">Annuleren X</a>
        <h1>Klant toevoegen</h1>
        
    </section>
    
    <section>
        <ol>
            
            <li <% if (current == 'gegevens') { %> style="opacity: 100%;"<% } %>>
                <% if (current == 'sdgs') { %> <a href="/klant-toevoegen-gegevens/<%= companies.id %>">
                    <p>1. Klant <span>toevoegen</span></p></a>
                <% } %>
                <% if (current == 'scores') { %> <a href="/klant-toevoegen-gegevens/<%= companies.id %>">
                    <p>1. Klant <span>toevoegen</span></p></a>
                <% } %>
                <% if (current == 'gegevens') { %>
                    <p>1. Klant <span>toevoegen</span></p>
                <% } %>

                
                
            </li>
            
            <li <% if (current == 'sdgs') { %> style="opacity: 100%;"<% } %>>
                <% if (current == 'scores') { %> 
                    <a href="/klant-toevoegen-sdgs/<%= companies.id %>">
                        <p>2. SDG's <span>kiezen</span></p>
                    </a>
                <% }else{ %>
                    <p>2. SDG's <span>kiezen</span></p>
                <% } %>
                </li>
            <li <% if (current == 'scores') { %> style="opacity: 100%;"<% } %>><p>3. Scores <span>toevoegen</span></p></li>
        </ol>

        <div class="dots-progress">
            <div></div>
            <div></div>
            <div></div>
        </div>
        <label>
            <% if (current == 'gegevens') { %>
                <progress id="file" value="5" max="100" list="progress"> 0% </progress>
            <% } %>
            <% if (current == 'sdgs') { %>
                <progress id="file" value="33" max="100" list="progress"> 33% </progress>
            <% } %>
            <% if (current == 'scores') { %>
                <progress id="file" value="66" max="100" list="progress"> 66% </progress>
            <% } %>
            
        </label>
    </section>
</header>